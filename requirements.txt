# FanSale Bot Requirements

# Core dependencies
undetected-chromedriver>=3.5.5
selenium-wire>=5.1.0
python-dotenv>=1.0.0

# Optional (for statistics)
# numpy>=1.24.0  # Not needed in simplified version