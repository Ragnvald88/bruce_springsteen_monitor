# Core requirements only
undetected-chromedriver>=3.5.0
selenium>=4.0.0
rich>=13.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional for future features
# aiohttp>=3.9.0  # For async HTTP if needed
# httpx>=0.25.0   # Alternative HTTP client
